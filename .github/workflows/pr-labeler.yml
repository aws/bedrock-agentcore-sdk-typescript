name: PR Labeler

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  call:
    uses: aws/agentcore-devx-devtools/.github/workflows/reusable-pr-labeler.yml@458c0a684af0f9e3a013ec05cd23851def4f9cab
    secrets: inherit
